# Environment variables for ClaudeCraftShell

# API Base URL (for web mode)
# This is the URL where the src-server is running
# Default: http://localhost:3000
VITE_API_URL=http://localhost:3000

# Build mode (optional, usually determined by script)
# Options: 'web' or 'tauri'
# VITE_MODE=web

# Tauri-specific variables (set automatically by Tauri)
# TAURI_PLATFORM=macos|windows|linux
# TAURI_FAMILY=unix|windows
# TAURI_DEBUG=1|0
